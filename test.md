1. 新建一个 c 文件，输出 "hello world"。
> 考察一个 c 程序最简单包含的内容。

2. 分别使用 while 和 for 两种循环，打印出 1.1, 2.2, 3.3, ... , 9.9, 10.10
> 考察 while 和 for 循环的使用，打印函数的占位符使用，简单的规律观察。

3. 创建一个包含 10 个元素的数组，并初始化为 1.1, 2.2, 3.3, ... , 9.9, 10.10，然后输出每一个元素。
> 考察数组的类型选择，初始化数组，使用数组。

4. 计算 1, 2, ..., 99, 100 的和。
